// This is a placeholder - actual generation happens on backend
export const createArticleFromTitle = async (title) => {
  // This function is called from BlogEditor but the actual work
  // is done by the backend API endpoint /blog/generate
  return null;
};
